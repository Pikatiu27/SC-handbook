# Geo58 — unified parameter lookup

## 1. Scope
18 September 2026; cache 20260918geo58. User approved material/state selection followed by one parameter table with inline sources. Both entries updated; Local / Unreleased.

## 2. Verdict
One fixed result region replaces numeric overview tables, moving details and the separate Full parameter reference. The picker retains all 22 state choices without presenting a second set of numeric results.

## 3. Must modify — completed
All original source-parameter rows represented once in the unified model. Source conditions retained in row-level evidence; unclassified permeability envelope stays in its source note. Main values remain source-qualified and no design adoption is added.

## 4. Should modify — completed
Refinement precedes results. Additional classification, test and project requirements use groups in the same table. Source tables for shaft friction and anchor bond separated and placed after their corresponding rows. Identical project condition/source sentences are not repeated.

## 5. Added
Unified-row model exposes source labels for coverage testing. Tests check one representation per original source parameter across clay, sand, gravel, rock and soil-like weathering. Existing factor, no-transfer and missing-range tests retained.

## 6. Simplified
Removed four numeric comparison tables, separate inventory and jump navigation. Removed overview-only rendering data. No parameter dataset deletion. Source reference tables remain supporting evidence, not another query entry.

## 7. Limits
One table is longer because project requirements and tests are now discoverable in place. Additional source tables are folded. No new coverage for gravel shaft resistance, rock socket resistance or soil-anchor bond. Existing independent, standards currency, source-specific publication, NSW/WA source and full manual-accessibility gates remain open.

## 8. Unchanged
Numeric dataset SHA256 matches the Geo56 baseline. Existing source values, query assumptions and engineering boundaries remain. Dirty changes preserved; no commit, push or publication.

## 9. Evidence
50 automated test files passed. Public build has 43 allowlisted files; Geo remains excluded. Live main: single parameter table, 22 state choices, very-dense sand factors, removal of old inventory, deselect/reselect and gravel-to-sand refinement verified. Desktop and 390px live screenshots inspected; no horizontal overflow at 390px. Standalone confirmed 22 choices and one selected table. Screenshots were live observations, not archived full-page certification. Viewport restored.

## 10. Delivery
http://127.0.0.1:4174/?preview=geo58#geo. Main and standalone use the same unified renderer.
